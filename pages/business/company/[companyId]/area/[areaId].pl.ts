import de from './[areaId].de'

const pl: typeof de = {
  backLinkText: 'Checkins',
  lastCheckins: 'Checkins z ostatnich 24 godzin. Aktualizuje się automatycznie',
  checkedIn: 'zameldowany',
  checkedOut: 'wymeldowany',
}

export default pl
