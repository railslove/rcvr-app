import _404de from './404.de'

const en: typeof _404de = {
  title: 'Page not found',
  message:
    "The page you called does not exist. Shouldn't this have happened? Then write us a mail to",
}

export default en
